import { ClientInfo } from './client-info';

describe('ClientInfo', () => {
  it('should create an instance', () => {
    expect(new ClientInfo('','','','','','','','','','')).toBeTruthy();
  });
});
