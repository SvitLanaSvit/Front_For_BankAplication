export class ClientInfo {
  constructor(public id: string, public status: string, public taxCode: string, public firstName: string, public lastName: string, public email: string, public address: string, public phone: string, public balance: string, public currencyCode: string){}
}
