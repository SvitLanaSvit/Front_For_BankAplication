export class Product {
  constructor(public name: string, public status: string, public currencyCode: string, public interestRate: string, public productLimit: string, public createdAt: string, public updatedAt: string, public managerId: string, public id: string = ''){}
}
