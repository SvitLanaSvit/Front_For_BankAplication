export class Agreement {
  constructor(public interestRate: string, public status: string, public sum: string, public createdAt: string, public updatedAt: string, public productId: string, public accountId: string, public id: string = ''){}
}
