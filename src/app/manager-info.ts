export class ManagerInfo {
  constructor(public id: string, public firstName: string, public lastName: string, public status: string, public productId: string, public name: string, public productLimit: string){}
}
