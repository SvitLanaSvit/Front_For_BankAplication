export class Account {
  constructor(public name: string, public type: string, public status: string, public balance: string, public currencyCode: string, public createdAt: string, public updatedAt: string, public clientId: string, public id: string = ''){}
}
